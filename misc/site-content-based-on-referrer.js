var docRef = document.referrer;
var refDomain = docRef.match(/:\/\/(.[^/]+/)[1];

if (refDomain == 'chiliklaus.dk') {
	// hvis du kommer fra chiliklaus.dk får du dette stærke indhold...
}
if (refDomain == 'marketingcamp.dk') {
	// ;-)
}
